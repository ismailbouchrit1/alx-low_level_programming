#include"main.h"

/**
 * _abs - calculate the absolute value
 * @n: number to calculate the absolute value
 * Return: 0 or n or -n
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	} else if (n < 0)
	{
		return (-n);
	} else if (n == 0)

	return (0);
}
